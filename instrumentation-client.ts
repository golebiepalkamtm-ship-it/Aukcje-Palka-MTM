// TYMCZASOWO WYŁĄCZONE - powodowało podwójną inicjalizację
// Sentry jest inicjalizowane tylko po stronie serwera (sentry.server.config.ts)
// Client side Sentry jest wyłączone do czasu rozwiązania problemu z podwójną inicjalizacją

// export const onRouterTransitionStart = undefined;
